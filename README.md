# linux-monitoring-backup

```
├── monitor.sh      # Monitors CPU, memory, and disk usage
├── backup.sh       # Creates timestamped compressed backups
├── logs/
│   └── system.log  # Log file generated by monitor.sh
├── backups/        # Stores compressed backup archives
└── README.md       # Project documentation and instructions

```